# .env.template — Kopyalayın: cp .env.template .env
# !! .env dosyasını asla git'e commit etmeyin !!

# ── TMDB API ──────────────────────────────────────────────────────
TMDB_API_KEY=your_tmdb_api_key_here
# TMDB_BEARER_TOKEN=your_bearer_token_here  # alternatif
TMDB_LANGUAGE=tr-TR

# ── HuggingFace ───────────────────────────────────────────────────
# PyAnnote speaker-diarization için (gated model)
# HF_TOKEN=hf_your_token_here

# ── Araç Yolları ──────────────────────────────────────────────────
# Linux/Mac'te genellikle PATH'ten bulunur, değiştirmeye gerek yok
# FFMPEG_BIN_DIR=/usr/local/bin

# Windows'ta:
# FFMPEG_BIN_DIR=F:\Source\ffmpeg\bin
# GOOGLE_JSON_PATH=F:\docs\keys\google_vid.json
# LOGOLAR_DIR=F:\Source\Logo

# ── Turkish Name DB ───────────────────────────────────────────────
# NAME_DB_PATH=/path/to/isimler.sql

# ── venv_audio (ses pipeline subprocess) ─────────────────────────
# VENV_AUDIO_PYTHON=/path/to/venv_audio/bin/python
# AUDIO_WORKER_SCRIPT=/path/to/Project/core/audio_worker.py
